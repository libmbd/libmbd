all:
	@$(MAKE) -C $(BLDDIR)

clean:
	@$(MAKE) -C $(BLDDIR) clean

distclean: clean
	-rm -rf $(BLDDIR)
	-rm -f config.status Makefile
	-rm -f $(wildcard *.mod)

$(BLDDIR)/%: %
	cp $< $@

Makefile: Makefile.in Makefile.build configure.py
	./configure.py
